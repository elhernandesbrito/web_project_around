/*
Crie a classe Card, que cria um cartão com texto e um link de imagem, de acordo com os seguintes requisitos:

    Utiliza os dados do cartão - texto e link para a imagem - e um seletor de elementos de template como parâmetros para o construtor.
    Possui métodos privados para trabalhar com marcação e adicionar ouvintes de eventos.
    Possui métodos privados para cada manipulador de eventos.
    Possui um método público que devolve o elemento do cartão totalmente funcional, preenchido com dados.

Crie uma instância de classe Card para cada cartão.

Crie a classe FormValidator, que define as configurações para validação dos campos do formulário de acordo 
com os seguintes requisitos:

    Seu construtor possui dois parâmetros. O primeiro parâmetro é um objeto de configuração que armazena seletores
     e classes de formulário, e o segundo utiliza um elemento de formulário para ser validado.
    Possui métodos privados para processar o formulário, dentre os quais: verificar a validade do campo, 
    alterar o estado do botão Submit e adicionar todos os manipuladores necessários.
    Possui um método público enableValidation(), que habilita a validação do formulário.

Crie uma instância da classe FormValidator para cada formulário que deve ser validado.

O primeiro requisito é adicionar as classes Card e FormValidator ao código e garantir que cada uma delas execute uma tarefa específica.
Essas classes devem conter todos os códigos necessários para o desempenho de suas respectivas tarefas.

Card.js deve conter o código da classe Card.

As classes Card e FormValidator devem ser exportadas de seus arquivos correspondentes, 
e então você deve importá-las e implementá-las dentro do index.js.

Certifique-se de que os arquivos JS estejam conectados ao index.html como módulos.

*/











